let summary = [2, 3, 4, 5];

function sum(array) {
  return array.reduce((a, b) => a + b);
}

console.log(sum(summary));

// Se mer på: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reduce
